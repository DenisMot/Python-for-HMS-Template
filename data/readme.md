# data
Files in `data` will not be shared on GitHub, be cause they may contain sensitive information. These files will not be tracked by git,  because the `data` folder is listed in `.gitignore`.

Only files in `data/test_data` are shared on GitHub, because they are **non-sensitive data that can be safely shared**.
